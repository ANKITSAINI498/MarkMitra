import Modal from '../common/Modal';import Button from '../common/Button';import {useMarkdown} from '../../context/MarkdownContext';export default function DeleteModal({open,onClose,id,title}){const {deleteDocument}=useMarkdown();return <Modal open={open} onClose={onClose} title="Delete document"><p className="modal-copy">Delete <strong>{title}</strong>? This action cannot be undone.</p><div className="modal-actions"><Button onClick={onClose}>Cancel</Button><Button variant="danger" onClick={()=>{deleteDocument(id);onClose()}}>Delete document</Button></div></Modal>}
