import {useState} from 'react';import Topbar from './Topbar';import Sidebar from './Sidebar';import StatusBar from './StatusBar';import Toast from '../common/Toast';import {useMarkdown} from '../../context/MarkdownContext';export default function AppLayout({children}){const {settings}=useMarkdown();const [mobileOpen,setMobileOpen]=useState(false);return <div className={`app-shell theme-${settings.theme}`} style={{'--sidebar-width':`${settings.sidebarWidth}px`}}><div className="ambient ambient-a"></div><div className="ambient ambient-b"></div><Topbar onMenu={()=>setMobileOpen(v=>!v)}/><div className="workspace-grid"><Sidebar mobileOpen={mobileOpen} onClose={()=>setMobileOpen(false)}/>{children}</div><StatusBar/><Toast/></div>}
