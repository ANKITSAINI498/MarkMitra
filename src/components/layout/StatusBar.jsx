import {useMarkdown} from '../../context/MarkdownContext';import {stats} from '../../utils/markdownHelpers';export default function StatusBar(){const {activeDocument}=useMarkdown();const s=stats(activeDocument?.content||'');return <footer className="statusbar"><span>Markdown</span><span>UTF-8</span><span>{s.words.toLocaleString()} words</span><span>{s.characters.toLocaleString()} chars</span><span>{s.lines} lines</span><span>{s.readingTime} min read</span><span className="status-spacer"></span><span>Local workspace</span></footer>}
