import {useMemo,useState} from 'react';import {AnimatePresence,motion} from 'framer-motion';import {FileText,Plus,Star,Clock3,Archive,LayoutTemplate,X,MoreHorizontal,ArchiveRestore,Copy,Trash2,Pencil,RotateCcw} from 'lucide-react';import SearchBox from '../common/SearchBox';import Button from '../common/Button';import {useMarkdown} from '../../context/MarkdownContext';export default function Sidebar({mobileOpen,onClose}){const {documents,activeId,setActiveId,createDocument,deleteDocument,renameDocument,duplicateDocument,toggle,search,setSearch}=useMarkdown();const [filter,setFilter]=useState('all');const [menu,setMenu]=useState(null);const filtered=useMemo(()=>documents.filter(d=>{const q=search.toLowerCase();const hit=!q||d.title.toLowerCase().includes(q)||d.content.toLowerCase().includes(q)||d.tags?.some(t=>t.toLowerCase().includes(q));const cat=filter==='all'||(filter==='favorites'&&d.favorite)||(filter==='recent'&&!d.archived)||(filter==='archived'&&d.archived);return hit&&cat}),[documents,search,filter]);const nav=[['all','All Documents',FileText],['favorites','Favorites',Star],['recent','Recent',Clock3],['archived','Archived',Archive]];return <aside className={`sidebar ${mobileOpen?'open':''}`}><div className="side-head"><span>Workspace</span><button className="icon-btn mobile-close" onClick={onClose}><X/></button></div><Button variant="primary" className="new-doc" onClick={()=>createDocument()}><Plus size={17}/> New Document</Button><SearchBox value={search} onChange={setSearch} placeholder="Search documents..."/><nav className="side-nav">{nav.map(([k,label,Icon])=><button key={k} className={filter===k?'active':''} onClick={()=>setFilter(k)}><Icon size={16}/><span>{label}</span><b>{k==='all'?documents.length:documents.filter(d=>k==='favorites'?d.favorite:k==='recent'?!d.archived:d.archived).length}</b></button>)}</nav><div className="list-head"><span>Documents</span><button className="icon-btn" onClick={()=>window.dispatchEvent(new Event('markflow:templates'))}><LayoutTemplate size={16}/></button></div><div className="doc-list">{filtered.map(d=><div className={`doc-item ${activeId===d.id?'active':''}`} key={d.id} onClick={()=>{setActiveId(d.id);onClose?.()}}><div className="doc-icon"><FileText size={16}/></div><div className="doc-meta"><strong>{d.title}</strong><small>{new Date(d.updatedAt).toLocaleDateString(undefined,{month:'short',day:'numeric'})}</small></div><button className="star-btn" onClick={e=>{e.stopPropagation();toggle(d.id,'favorite')}} aria-label="Favorite"><Star size={14} fill={d.favorite?'currentColor':'none'}/></button><button className="more-btn" onClick={e=>{e.stopPropagation();setMenu(menu===d.id?null:d.id)}}><MoreHorizontal size={16}/></button>{menu===d.id&&<div className="doc-menu" onClick={e=>e.stopPropagation()}><button onClick={()=>{const n=prompt('Rename document',d.title.replace(/\.md$/,''));if(n)renameDocument(d.id,n);setMenu(null)}}><Pencil size={14}/>Rename</button><button onClick={()=>{duplicateDocument(d.id);setMenu(null)}}><Copy size={14}/>Duplicate</button><button onClick={()=>{toggle(d.id,'archived');setMenu(null)}}>{d.archived?<ArchiveRestore size={14}/>:<Archive size={14}/>} {d.archived?'Restore':'Archive'}</button><button className="danger" onClick={()=>{if(confirm(`Delete ${d.title}?`))deleteDocument(d.id);setMenu(null)}}><Trash2 size={14}/>Delete</button></div>}</div>)}{!filtered.length&&<div className="empty-mini"><FileText size={22}/><span>No matching documents</span></div>}</div><div className="side-bottom"><button onClick={()=>window.dispatchEvent(new Event('markflow:templates'))}><LayoutTemplate size={16}/> Templates</button><button onClick={()=>window.dispatchEvent(new Event('markflow:settings'))}><SettingsIcon/> Settings</button></div></aside>}
function SettingsIcon(){return <span style={{fontSize:15}}>⚙</span>}
