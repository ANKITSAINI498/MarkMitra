import {AnimatePresence,motion} from 'framer-motion';import {Check,AlertTriangle} from 'lucide-react';import {useMarkdown} from '../../context/MarkdownContext';export default function Toast(){const {toast}=useMarkdown();return <AnimatePresence>{toast&&<motion.div className={`mf-toast ${toast.type==='error'?'error':''}`} initial={{opacity:0,y:16,x:20}} animate={{opacity:1,y:0}} exit={{opacity:0,x:20}}>{toast.type==='error'?<AlertTriangle size={17}/>:<Check size={17}/>}<span>{toast.message}</span></motion.div>}</AnimatePresence>}
